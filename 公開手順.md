# 公開のしかた・更新のしかた（GitHub Pages）

## いまの状態

**すでに公開ずみです。** 2026-09-20 に Claude が GitHub へアップロードして、公開のスイッチも入れました。

- 公開URL： **https://akirahaan-blip.github.io/pksl.dream-counter/**
- ファイルの置き場所（リポジトリ）： https://github.com/akirahaan-blip/pksl.dream-counter

自分用のページなので、Google などの検索結果には出ない設定（`noindex`）にしてあります。
URL を知っている人だけが開けます。記録した数字は、使っているスマホ／パソコンのブラウザの中にだけ保存されます（GitHub には送られません）。

---

## スマホで使うとき（最初の1回だけ）

1. スマホのブラウザ（Safari や Chrome）で公開URLを開く
2. **ホーム画面に追加**すると、アプリみたいにアイコンから開けます
   - iPhone（Safari）：画面下の「共有」ボタン（□に↑）→「ホーム画面に追加」→「追加」
   - Android（Chrome）：右上の「⋮」→「ホーム画面に追加」→「追加」
3. ホーム画面にムシャーナのアイコンが出ればOK

---

## ページを直したとき（更新のしかた）

パソコンの `index.html` を直しただけでは、公開中のサイトは変わりません。
GitHub に「アップロードし直す」必要があります。

**Claude に「ドリームダービーを公開に反映して」と言えば、Claude がやります。** 自分でやる場合は：

1. PowerShell を開く
2. 次の3行を1行ずつ貼り付けて、それぞれ Enter

   ```
   cd "C:\Users\oauao\Claude\ポケスリ\pksl.dream-counter"
   git add -A; git commit -m "直した内容をここに書く"
   git push
   ```

3. 1〜2分待って、ページを開き直す（スマホなら下に引っぱって更新）

> **うまくいったかの見分け方**：
> `git push` のあとにエラー（赤い文字で `error` や `rejected`）が出なければ成功です。
> 反映されていないように見えたら、少し待ってからもう一度開き直してください。
